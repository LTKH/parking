name: Release

#on:
#  push:
#    tags:
#      - 'v*'

on:
  push:
    branches: ['main']

jobs:
  release:
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Set up Go 1.19
        uses: actions/setup-go@v3
        with:
          go-version: '1.19.2'
      - run: go get github.com/webview/webview
      - run: GOOS=windows GOARCH=amd64 go build parking.go -o /tmp/parking-windows-amd64.exe 
      #- name: Create Release
      #  uses: actions/create-release@v0.0.6
      #  id: create_release
      #  env:
      #    GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      #  with:
      #    tag_name: ${{ github.ref }}
      #    release_name: Release ${{ github.ref }}
      #    draft: false
      #    prerelease: false
      #- name: Upload Release Assets
      #  uses: glentiki/xbin-release-action@v0.0.6
      #  env:
      #    GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      #  with:
      #    upload_url: ${{ steps.create_release.outputs.upload_url }} 
      #    assets_path: ./build